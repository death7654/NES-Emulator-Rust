# NES-Emulator-Rust
A Nes Emulator made in Rust

## Build Instructions
1. Set minimium cmake version
```
$env:CMAKE_POLICY_VERSION_MINIMUM = "3.5"
```

2. build and run the emulator
```
cargo run
```

